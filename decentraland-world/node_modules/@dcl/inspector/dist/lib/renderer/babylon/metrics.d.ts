import type { Scene } from '@babylonjs/core';
import type { Entity } from '@dcl/ecs';
import type { SceneContext } from '../../babylon/decentraland/SceneContext';
export declare function computeSceneMetrics(scene: Scene): {
    triangles: number;
    bodies: number;
    materials: number;
    textures: number;
};
export declare function computeEntitiesOutsideLayout(scene: Scene, context: SceneContext): Entity[];
