"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PBAvatarEmoteCommand = exports.EmoteState = exports.protobufPackage = void 0;
exports.emoteStateFromJSON = emoteStateFromJSON;
exports.emoteStateToJSON = emoteStateToJSON;
/* eslint-disable */
const minimal_1 = __importDefault(require("protobufjs/minimal"));
const avatar_mask_gen_1 = require("./common/avatar_mask.gen");
exports.protobufPackage = "decentraland.sdk.components";
/** EmoteState describes the lifecycle state of an emote playback. */
var EmoteState;
(function (EmoteState) {
    /**
     * ES_STARTED - ES_STARTED indicates the emote started playing.
     * This is the zero value and is used for backward compatibility — entries
     * written by older explorers (field absent) read as "started".
     */
    EmoteState[EmoteState["ES_STARTED"] = 0] = "ES_STARTED";
    /** ES_FINISHED - ES_FINISHED indicates a non-looping emote completed naturally. */
    EmoteState[EmoteState["ES_FINISHED"] = 1] = "ES_FINISHED";
    /**
     * ES_INTERRUPTED - ES_INTERRUPTED indicates playback was cancelled (movement, teleport,
     * another emote superseding it, explicit stop, or scene change).
     */
    EmoteState[EmoteState["ES_INTERRUPTED"] = 2] = "ES_INTERRUPTED";
    EmoteState[EmoteState["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(EmoteState || (exports.EmoteState = EmoteState = {}));
function emoteStateFromJSON(object) {
    switch (object) {
        case 0:
        case "ES_STARTED":
            return EmoteState.ES_STARTED;
        case 1:
        case "ES_FINISHED":
            return EmoteState.ES_FINISHED;
        case 2:
        case "ES_INTERRUPTED":
            return EmoteState.ES_INTERRUPTED;
        case -1:
        case "UNRECOGNIZED":
        default:
            return EmoteState.UNRECOGNIZED;
    }
}
function emoteStateToJSON(object) {
    switch (object) {
        case EmoteState.ES_STARTED:
            return "ES_STARTED";
        case EmoteState.ES_FINISHED:
            return "ES_FINISHED";
        case EmoteState.ES_INTERRUPTED:
            return "ES_INTERRUPTED";
        case EmoteState.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBasePBAvatarEmoteCommand() {
    return { emoteUrn: "", loop: false, timestamp: 0, mask: undefined, state: undefined };
}
var PBAvatarEmoteCommand;
(function (PBAvatarEmoteCommand) {
    function encode(message, writer = minimal_1.default.Writer.create()) {
        if (message.emoteUrn !== "") {
            writer.uint32(10).string(message.emoteUrn);
        }
        if (message.loop === true) {
            writer.uint32(16).bool(message.loop);
        }
        if (message.timestamp !== 0) {
            writer.uint32(24).uint32(message.timestamp);
        }
        if (message.mask !== undefined) {
            writer.uint32(32).int32(message.mask);
        }
        if (message.state !== undefined) {
            writer.uint32(40).int32(message.state);
        }
        return writer;
    }
    PBAvatarEmoteCommand.encode = encode;
    function decode(input, length) {
        const reader = input instanceof minimal_1.default.Reader ? input : minimal_1.default.Reader.create(input);
        let end = length === undefined ? reader.len : reader.pos + length;
        const message = createBasePBAvatarEmoteCommand();
        while (reader.pos < end) {
            const tag = reader.uint32();
            switch (tag >>> 3) {
                case 1:
                    if (tag !== 10) {
                        break;
                    }
                    message.emoteUrn = reader.string();
                    continue;
                case 2:
                    if (tag !== 16) {
                        break;
                    }
                    message.loop = reader.bool();
                    continue;
                case 3:
                    if (tag !== 24) {
                        break;
                    }
                    message.timestamp = reader.uint32();
                    continue;
                case 4:
                    if (tag !== 32) {
                        break;
                    }
                    message.mask = reader.int32();
                    continue;
                case 5:
                    if (tag !== 40) {
                        break;
                    }
                    message.state = reader.int32();
                    continue;
            }
            if ((tag & 7) === 4 || tag === 0) {
                break;
            }
            reader.skipType(tag & 7);
        }
        return message;
    }
    PBAvatarEmoteCommand.decode = decode;
    function fromJSON(object) {
        return {
            emoteUrn: isSet(object.emoteUrn) ? String(object.emoteUrn) : "",
            loop: isSet(object.loop) ? Boolean(object.loop) : false,
            timestamp: isSet(object.timestamp) ? Number(object.timestamp) : 0,
            mask: isSet(object.mask) ? (0, avatar_mask_gen_1.avatarMaskFromJSON)(object.mask) : undefined,
            state: isSet(object.state) ? emoteStateFromJSON(object.state) : undefined,
        };
    }
    PBAvatarEmoteCommand.fromJSON = fromJSON;
    function toJSON(message) {
        const obj = {};
        message.emoteUrn !== undefined && (obj.emoteUrn = message.emoteUrn);
        message.loop !== undefined && (obj.loop = message.loop);
        message.timestamp !== undefined && (obj.timestamp = Math.round(message.timestamp));
        message.mask !== undefined && (obj.mask = message.mask !== undefined ? (0, avatar_mask_gen_1.avatarMaskToJSON)(message.mask) : undefined);
        message.state !== undefined &&
            (obj.state = message.state !== undefined ? emoteStateToJSON(message.state) : undefined);
        return obj;
    }
    PBAvatarEmoteCommand.toJSON = toJSON;
    function create(base) {
        return PBAvatarEmoteCommand.fromPartial(base !== null && base !== void 0 ? base : {});
    }
    PBAvatarEmoteCommand.create = create;
    function fromPartial(object) {
        var _a, _b, _c, _d, _e;
        const message = createBasePBAvatarEmoteCommand();
        message.emoteUrn = (_a = object.emoteUrn) !== null && _a !== void 0 ? _a : "";
        message.loop = (_b = object.loop) !== null && _b !== void 0 ? _b : false;
        message.timestamp = (_c = object.timestamp) !== null && _c !== void 0 ? _c : 0;
        message.mask = (_d = object.mask) !== null && _d !== void 0 ? _d : undefined;
        message.state = (_e = object.state) !== null && _e !== void 0 ? _e : undefined;
        return message;
    }
    PBAvatarEmoteCommand.fromPartial = fromPartial;
})(PBAvatarEmoteCommand || (exports.PBAvatarEmoteCommand = PBAvatarEmoteCommand = {}));
function isSet(value) {
    return value !== null && value !== undefined;
}
//# sourceMappingURL=avatar_emote_command.gen.js.map