/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { ExplorerUi, explorerUiFromJSON, explorerUiToJSON } from "./common/explorer_ui.gen";

export const protobufPackage = "decentraland.sdk.components";

/**
 * PBExplorerUiEventsResult transports the lifecycle events of fullscreen explorer panels — a panel was
 * opened, a panel was closed. It is a grow only value set appended to the scene root entity, so every
 * event of a tick is delivered and none overwrites another.
 */
export interface PBExplorerUiEventsResult {
  /** The panel that the event refers to */
  ui: ExplorerUi;
  /** The scene tick when the event occurred */
  timestamp: number;
  event?: { $case: "opened"; opened: PBExplorerUiEventsResult_UiOpened } | {
    $case: "closed";
    closed: PBExplorerUiEventsResult_UiClosed;
  } | undefined;
}

/** Emitted when a fullscreen explorer panel is opened. */
export interface PBExplorerUiEventsResult_UiOpened {
}

/** Emitted when a fullscreen explorer panel is closed. */
export interface PBExplorerUiEventsResult_UiClosed {
}

function createBasePBExplorerUiEventsResult(): PBExplorerUiEventsResult {
  return { ui: 0, timestamp: 0, event: undefined };
}

export namespace PBExplorerUiEventsResult {
  export function encode(message: PBExplorerUiEventsResult, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.ui !== 0) {
      writer.uint32(8).int32(message.ui);
    }
    if (message.timestamp !== 0) {
      writer.uint32(16).uint32(message.timestamp);
    }
    switch (message.event?.$case) {
      case "opened":
        PBExplorerUiEventsResult_UiOpened.encode(message.event.opened, writer.uint32(82).fork()).ldelim();
        break;
      case "closed":
        PBExplorerUiEventsResult_UiClosed.encode(message.event.closed, writer.uint32(90).fork()).ldelim();
        break;
    }
    return writer;
  }

  export function decode(input: _m0.Reader | Uint8Array, length?: number): PBExplorerUiEventsResult {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePBExplorerUiEventsResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.ui = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.timestamp = reader.uint32();
          continue;
        case 10:
          if (tag !== 82) {
            break;
          }

          message.event = {
            $case: "opened",
            opened: PBExplorerUiEventsResult_UiOpened.decode(reader, reader.uint32()),
          };
          continue;
        case 11:
          if (tag !== 90) {
            break;
          }

          message.event = {
            $case: "closed",
            closed: PBExplorerUiEventsResult_UiClosed.decode(reader, reader.uint32()),
          };
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  }

  export function fromJSON(object: any): PBExplorerUiEventsResult {
    return {
      ui: isSet(object.ui) ? explorerUiFromJSON(object.ui) : 0,
      timestamp: isSet(object.timestamp) ? Number(object.timestamp) : 0,
      event: isSet(object.opened)
        ? { $case: "opened", opened: PBExplorerUiEventsResult_UiOpened.fromJSON(object.opened) }
        : isSet(object.closed)
        ? { $case: "closed", closed: PBExplorerUiEventsResult_UiClosed.fromJSON(object.closed) }
        : undefined,
    };
  }

  export function toJSON(message: PBExplorerUiEventsResult): unknown {
    const obj: any = {};
    message.ui !== undefined && (obj.ui = explorerUiToJSON(message.ui));
    message.timestamp !== undefined && (obj.timestamp = Math.round(message.timestamp));
    message.event?.$case === "opened" &&
      (obj.opened = message.event?.opened
        ? PBExplorerUiEventsResult_UiOpened.toJSON(message.event?.opened)
        : undefined);
    message.event?.$case === "closed" &&
      (obj.closed = message.event?.closed
        ? PBExplorerUiEventsResult_UiClosed.toJSON(message.event?.closed)
        : undefined);
    return obj;
  }

  export function create<I extends Exact<DeepPartial<PBExplorerUiEventsResult>, I>>(
    base?: I,
  ): PBExplorerUiEventsResult {
    return PBExplorerUiEventsResult.fromPartial(base ?? {});
  }

  export function fromPartial<I extends Exact<DeepPartial<PBExplorerUiEventsResult>, I>>(
    object: I,
  ): PBExplorerUiEventsResult {
    const message = createBasePBExplorerUiEventsResult();
    message.ui = object.ui ?? 0;
    message.timestamp = object.timestamp ?? 0;
    if (object.event?.$case === "opened" && object.event?.opened !== undefined && object.event?.opened !== null) {
      message.event = { $case: "opened", opened: PBExplorerUiEventsResult_UiOpened.fromPartial(object.event.opened) };
    }
    if (object.event?.$case === "closed" && object.event?.closed !== undefined && object.event?.closed !== null) {
      message.event = { $case: "closed", closed: PBExplorerUiEventsResult_UiClosed.fromPartial(object.event.closed) };
    }
    return message;
  }
}

function createBasePBExplorerUiEventsResult_UiOpened(): PBExplorerUiEventsResult_UiOpened {
  return {};
}

export namespace PBExplorerUiEventsResult_UiOpened {
  export function encode(_: PBExplorerUiEventsResult_UiOpened, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  }

  export function decode(input: _m0.Reader | Uint8Array, length?: number): PBExplorerUiEventsResult_UiOpened {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePBExplorerUiEventsResult_UiOpened();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  }

  export function fromJSON(_: any): PBExplorerUiEventsResult_UiOpened {
    return {};
  }

  export function toJSON(_: PBExplorerUiEventsResult_UiOpened): unknown {
    const obj: any = {};
    return obj;
  }

  export function create<I extends Exact<DeepPartial<PBExplorerUiEventsResult_UiOpened>, I>>(
    base?: I,
  ): PBExplorerUiEventsResult_UiOpened {
    return PBExplorerUiEventsResult_UiOpened.fromPartial(base ?? {});
  }

  export function fromPartial<I extends Exact<DeepPartial<PBExplorerUiEventsResult_UiOpened>, I>>(
    _: I,
  ): PBExplorerUiEventsResult_UiOpened {
    const message = createBasePBExplorerUiEventsResult_UiOpened();
    return message;
  }
}

function createBasePBExplorerUiEventsResult_UiClosed(): PBExplorerUiEventsResult_UiClosed {
  return {};
}

export namespace PBExplorerUiEventsResult_UiClosed {
  export function encode(_: PBExplorerUiEventsResult_UiClosed, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  }

  export function decode(input: _m0.Reader | Uint8Array, length?: number): PBExplorerUiEventsResult_UiClosed {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePBExplorerUiEventsResult_UiClosed();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  }

  export function fromJSON(_: any): PBExplorerUiEventsResult_UiClosed {
    return {};
  }

  export function toJSON(_: PBExplorerUiEventsResult_UiClosed): unknown {
    const obj: any = {};
    return obj;
  }

  export function create<I extends Exact<DeepPartial<PBExplorerUiEventsResult_UiClosed>, I>>(
    base?: I,
  ): PBExplorerUiEventsResult_UiClosed {
    return PBExplorerUiEventsResult_UiClosed.fromPartial(base ?? {});
  }

  export function fromPartial<I extends Exact<DeepPartial<PBExplorerUiEventsResult_UiClosed>, I>>(
    _: I,
  ): PBExplorerUiEventsResult_UiClosed {
    const message = createBasePBExplorerUiEventsResult_UiClosed();
    return message;
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Array<infer U> ? Array<DeepPartial<U>> : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
