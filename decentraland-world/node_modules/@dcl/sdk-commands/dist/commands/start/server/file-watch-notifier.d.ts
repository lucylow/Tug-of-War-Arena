import { WebSocket } from 'ws';
import { PreviewComponents } from '../types';
import { ProjectUnion } from '../../../logic/project-validations';
/**
 * This function gets file modification events and sends them to all the connected
 * websockets, it is used to hot-reload assets of the scene.
 */
export declare function wireFileWatcherToWebSockets(components: Pick<PreviewComponents, 'fs' | 'ws' | 'logger'>, projectRoot: string, projectKind: ProjectUnion['kind']): Promise<void>;
/**
 * @deprecated legacy JSON protocol, consumed by Bevy and Godot explorers
 */
export declare function __LEGACY__updateScene(dir: string, clients: Set<WebSocket>, projectKind: ProjectUnion['kind']): void;
