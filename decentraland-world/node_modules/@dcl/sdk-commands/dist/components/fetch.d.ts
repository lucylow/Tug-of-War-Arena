export type IFetchComponent = {
    fetch(url: string, init?: Omit<RequestInit, 'dispatcher'> & {
        dispatcher?: unknown;
    }): Promise<Response>;
};
export declare function createFetchComponent(): IFetchComponent;
