/**
 * Mobile wallet linking uses MetaMask Connect's `preferredOpenLink` handler.
 * The deprecated `@walletconnect/react-native-dapp` package is intentionally not installed.
 */
export { MOBILE_WALLET_SCHEMES } from "@/lib/web3/config";
